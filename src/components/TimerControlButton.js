import React, {PropTypes} from 'react';

const TimerControlButton = props => {
  return (
    <div>

    </div>
  );
};

TimerControlButton.propTypes = {
  abililty : PropTypes.func.isRequired,
  label : PropTypes.string.isRequired
};

export default TimerControlButton;
